// Ambient declaration for the vendored nop-diff bundle (imported for its side
// effects only: it registers the <nop-diff> custom element and defines
// window.NopDiff — see NopDiffView.tsx for the typed surface we use).
export {};
