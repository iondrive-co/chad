{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\"file_path\".*Readme\\.md'; then echo '{\"decision\": \"block\", \"reason\": \"Use README.md (uppercase) instead of Readme.md\"}'; fi"
          }
        ]
      }
    ]
  }
}
